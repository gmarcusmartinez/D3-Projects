SVG Paths
<path M150 0 L75 200 L225 200 Z/>

M - move to
L - line to
Z - close path

Select Elements in DOM
- d3.select() 
- d3.selectAll()

Manipulating Selections
selection.style()
selection.attr()
selection.text()
selection.html()

Manipulating Selections w/ Callback functions

selection.style (property, callback(_, index))
selection.attr  (property, callback(_, index))
selection.text  (property, callback(_, index))
selection.html  (property, callback(_, index))

Event Listeners 
d3.on(eventType, callback)
d3.event.preventDefault()

Data Joins and Enter Selections
 - Manipulating Documents based on Data
 - Combine Elements with Data
 - attach each piece of data object with an element
 - enter()
  .append(element-type)
 - .exit()
  - elements no longer bound to data
  - .remove()
 
updatePattern
 - select parent element to .append() to
 - enter() not yet bound to data
 - exit() no longer bound to data
 - update() connected to data
 - merge()








